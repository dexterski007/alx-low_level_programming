#include "main.h"

/**
 *  print_numbers - checks if it is upper.
 *
 *
 * Return: multi
 *
 */

void print_numbers(void)
{
	int r = 0;
	int g = 48;

	while (r < 10)
{
	_putchar(g + r);
	r++;
}
_putchar(10);
}
